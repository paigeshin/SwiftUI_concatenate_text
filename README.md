# SwiftUI_concatenate_text


```swift

                 // In SwiftUI We can concatenate two ore more Text's...
                    (
                    
                        Text("PaigeSoftware")
                            .fontWeight(.bold)
                        
                        +
                        
                        Text("@_Paigesoftware")
                            .foregroundColor(.gray)
                        
                    )

```
